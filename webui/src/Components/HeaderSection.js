import { Layout } from 'antd';
import React from 'react';
const { Header } = Layout;

const HeaderSection = () => {
    return (
        <Header className="site-layout-background"
            style={{ padding: 0, color: '#ffffff', backgroundColor: '#081F2C' }}>
            Header
        </Header>
    )
}

export default HeaderSection
